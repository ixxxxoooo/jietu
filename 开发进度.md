# Jietu 开发进度

原生 macOS 截图工具（对标 CleanShot）。菜单栏常驻（LSUIElement）、
ScreenCaptureKit 冻结全屏 + 全屏遮罩选区。

## 已完成

### P0 骨架
- Xcode 工程（文件系统同步组）/ entitlements / LSUIElement 菜单栏应用
- Carbon 全局热键（`RegisterEventHotKey`，无需辅助功能权限）
- 权限 Onboarding：先注册 TCC 再引导重启

### P1 选区与输出
- 选区状态机 + 8 handle 调整 + 方向键微调 + Shift 正方形
- 窗口吸附高亮（点选整窗）/ 尺寸标签（放大镜 8x 与实时取色已在后续版本去掉）
- 精确裁剪 → 剪贴板（PNG+TIFF）/ 磁盘 / 快门音
- Quick Access 浮动预览（复制 / 保存 / 关闭 / 3s 自动关闭）

### 设置与测试（本轮新增）
- 偏好设置窗口 `Jietu/UI/Settings/`
  - `SettingsView`：快捷键、复制到剪贴板、快门音、保存到磁盘、保存位置、预览自动关闭
  - `HotkeyRecorderView`：本地事件监视器录制组合键（Esc 取消、需修饰键）
  - `SettingsWindowController`：菜单栏「偏好设置…」(⌘,) 打开
- 热键热重注册：改组合键后由 `AppDelegate.applyAreaCaptureHotkey` 注销旧键、注册新键
- 常量统一：`Theme.minimumSelectionSize` 复用 `SelectionGeometry.minimumSide`；
  `QuickAccessView.cardWidth` 复用 `Theme.quickAccessWidth`
- 单元测试 target `JietuTests`（Swift Testing，29 个用例）：
  - `SelectionGeometry` 矩形/锁比/调整/平移/命中
  - `DisplayGeometry` cg↔local 往返、Y 轴翻转
  - `DisplaySnapshot` effectiveScale、pixelPoint
  - `CaptureOutput` 裁剪 Y 翻转、空选区、PNG 编码、同名追加序号
  - `Hotkey` 显示串、`from(event:)`
  - `SettingsStore` 默认值与持久化
  - `AnnotationRenderer` 矩形描边、序号圆点、马赛克成块与作用域
- 命令行自检（DEBUG-only）：`--selftest-capture` / `--selftest-overlay` / `--selftest-onboarding`

### P2 标注与 Quick Access（本轮新增）
- 标注核心 `Jietu/Core/Annotation/`
  - `Annotation` / `AnnotationTool` / `RGBAColor`：几何量为图像像素坐标（原点左上）
  - `AnnotationRenderer`：把标注烘焙进底图；矩形/椭圆/箭头/文字/马赛克/序号；
    马赛克用「降采样后再无插值放大」，与导出同一路径
- 标注编辑器 `Jietu/UI/Annotation/`
  - `AnnotationEditorView`：工具栏（6 种工具 + 7 色 + 线宽 + 撤销/重做/清空 + 复制/保存/关闭）
  - 预览用降采样底图 + 同一渲染器，所见即所得
  - `AnnotationEditorWindowController`：通过 Quick Access 的「标注」按钮打开
- Quick Access 增强：
  - 缩略图可**拖拽导出**（提供临时 PNG 文件）
  - **钉图**：`PinWindowController` 把截图钉成可拖动浮窗，支持多张
    - 右上角配备 **Liquid Glass 原生毛玻璃关闭按钮**（`GlassControlButton: NSControl`，`NSVisualEffectView(blendingMode: .withinWindow, material: .hudWindow)`，带 0.5pt 高光折射描边、悬停微光与原生柔和投影）
    - 右下角实况文本（Live Text）按钮同步升级为统一 Liquid Glass 风格
    - 支持 `⌘W` 和 `Esc` 快捷关闭、`⌘C` 复制、`⌘0` 恢复 1:1 实际大小、双击关闭
    - 解决点击两次才生效的 Bug：实现 `acceptsFirstMouse(for:) -> true`，即使在未激活/后台窗口下也能单次首击即刻触发关闭
  - **预览自动关闭**改为读设置（1/3/5 秒 / 永不）

### 截图模式与菜单（本轮新增）
- 菜单新增：**区域 / 窗口 / 全屏 / 定时（3·5·10 秒）** 四种模式
  - 窗口截图：对标 capcap 与 CleanShot X 重构，进入全屏交互遮罩，支持相机光标、悬停圆角高亮（10pt）与一键选窗；支持按空格键在「窗口截图」与「自由框选」之间无缝切换；通过 `SCContentFilter(desktopIndependentWindow:)` 独立纯净捕获目标窗口（无遮挡），自动裁切圆角并渲染 macOS 拟物多层柔和深色阴影，设置页支持阴影开关与大小调节
  - 全屏截图：抓鼠标所在显示器，不弹遮罩
  - 定时截图：延时后走区域截图流程
- **最近截图**子菜单（`MenuBarController` 实现 `NSMenuDelegate`，每次弹出重建）：
  点条目在访达中定位；「清除记录」；记录持久化在 `SettingsStore`
- **打开截图文件夹**菜单项
- **登录时启动**：`LaunchAtLogin` 封装 `SMAppService.mainApp`，设置页开关，
  启动时同步系统状态
- 保存成功后自动写入「最近截图」

### 导出、图标与签名（本轮新增）
- **保存格式**：设置页可选 PNG / JPEG，JPEG 带质量滑块；`CaptureOutput.save` 支持
  `format` + `quality`，扩展名随格式变化
- **热键注册失败反馈**：改快捷键被占用时自动回滚到原组合键并弹窗提示；
  `applyAreaCaptureHotkey` 现在返回是否成功
- **App 图标**：`AppIcon.appiconset` 已补齐 16…1024 全套 PNG（脚本绘制：品牌蓝底 + 取景框/镜头）
- **签名**：工程 `CODE_SIGN_IDENTITY` 由不存在的「Jietu Development」改为自签名证书
  **Jietu**（登录钥匙串，`Authority=Jietu`）；自签名让 TCC 有稳定身份，权限不再因重编译丢失

### 交互与布局（本轮新增）
- **Quick Access 停靠位置**：设置页可选「右下角 / 左下角」
- **浮窗按钮改版**：功能按钮置于缩略图**四角**（复制 / 标注 / 钉图 / 关闭），
  **保存**居中；点击缩略图打开标注编辑器，拖拽仍可导出
- **预览自动关闭**新增 10 / 30 / 60 秒选项
- **钉图 / 编辑器默认原始大小**：`钉图` 与编辑器初始尺寸按「像素 ÷ 屏幕缩放」，
  超过屏幕 90% 才等比缩小
- **编辑器窗口可调整大小 + 缩放**：`zoom` 相对原始大小（1.0 = 原始），
  支持 适应 / 缩小 / 放大 / 原始，内容放入双轴 `ScrollView` 可滚动查看
- **编辑器新增钉图按钮**：把带标注的成图直接钉到屏幕
- **窗口吸附高亮**：区域截图里点选整窗后保持与悬停一致的蓝色高亮，不再「突然变色」

### 多热键体系（本轮新增）
- `HotkeyAction`：区域 / 窗口 / 全屏 / 定时四个动作，各自带默认组合键
  （`⌘⇧A` / `⌘⇧W` / `⌘⇧F` / `⌘⇧T`）
- `SettingsStore.hotkeys`：动作 → 组合键的映射，整体 JSON 持久化；
  旧版本的单条 `hotkey.areaCapture` 会在启动时自动迁移到区域截图
- `AppDelegate`：启动批量注册；改键时注销旧键再注册新键，
  被占用或与其它动作撞车时回滚到原组合键并弹窗说明是哪个动作失败
- 设置页「快捷键」分区改为四行，每行独立录制（`HotkeyRecorderView` 参数化标题/说明）

### 保存反馈（本轮新增）
- `CaptureNotifier`（`Core/Notifications/`）：`UNUserNotificationCenter` 通知，
  带图片附件做缩略图，点击在访达中定位刚存下的文件；无 bundle 的场景自动跳过
- 菜单栏图标：保持静默稳定，截图时不闪动
- 落盘收尾统一到 `AppDelegate.didSave(to:)`：记入最近截图 + 通知
- 设置项「保存后显示系统通知」（默认开）

### 文件名模板（本轮新增）
- `FilenameTemplate`（`Core/ImageIO/`）：`{date}` / `{time}` / `{datetime}` / `{counter}` 占位符，
  空模板回落默认、非法字符（`/` `:`）自动替换
- 默认模板 `Jietu {date} at {time}` 与历史命名完全一致，「最近截图」时间解析不受影响
- `{counter}` 按目录内同模板文件推断下一个序号；同名冲突仍兜底追加 `-N`
- 设置页「截图」分区新增模板输入框 + 示例预览 + 恢复默认

### 标注单条操作（本轮新增）
- `AnnotationStyle`：样式（颜色 / 线宽 / 字号 / 马赛克块）提取与套用，
  字号只作用于文字 / 气泡、块大小只作用于马赛克，不会互相污染
- 选中标注后右侧浮出操作列：置顶 `⇧⌘]` / 置底 `⇧⌘[` / 复制一份 `⌘D` /
  复制样式 `⌥⌘C` / 粘贴样式 `⌥⌘V` / 删除 `⌫`
- `⌫` 与工具栏垃圾桶都能删单条；复制序号 / 气泡时自动分配新号
- 顺手清掉标注编辑器里 `isCallout = false` 造成的死分支与两处未使用绑定警告

### 截后再裁剪（本轮新增）
- `AnnotationTool.crop`（仅标注编辑器窗口有；原地编辑工具栏不提供）
- `CropOperation`：裁剪框夹进图像范围并取整（小于 4px 视为误操作）、
  裁底图、把标注与擦除笔迹按新原点平移
- 交互：拖出裁剪框 → 框外压暗 + 虚线框 + 尺寸 / ✓ / ✗，回车确认、切工具即取消
- 编辑器底图改为可替换（`croppedImage`），`EditorSnapshot` 一起存底图，
  撤销 / 重做能连裁剪一起回滚，并重栅格化预览

### 新增标注工具（本轮新增）
- `line` 直线：两端可拖，命中按线段距离判定
- `blur` 模糊：`CIGaussianBlur` 只作用于底图对应区域，取一圈 padding 再裁回避免边缘发虚
- `magnifier` 放大镜：以框中心为锚点按倍率取源区域放大填满椭圆 + 描边
- 三个工具在标注编辑器与原地编辑工具栏都可用；选中后选项条出现
  模糊半径 / 放大倍率滑块，样式复制也带上各自参数

### 默认样式记忆（本轮新增）
- `AnnotationDefaults`（工具 / 颜色 / 线宽 / 字号 / 马赛克块 / 模糊半径 / 放大倍率 / 橡皮大小），
  整体 JSON 存在 `annotation.defaults`，读取时 `sanitized` 夹回滑块范围防存档损坏
- `AnnotationTool` / `RGBAColor` 补 `Codable`
- 标注编辑器开窗时按默认样式初始化，`.onChange(of: currentDefaults)` 回写
- 原地工具栏：`OverlayCanvasView.annotationDefaults` 播种，用 `withObservationTracking`
  监听工具 / 颜色 / 参数变化回报；`OverlayCoordinator` 转发给 AppDelegate 落盘
- 设置页「标注」分区显示当前默认样式摘要 + 恢复默认

### 滚动长图（本轮新增）
- `ScrollStitcher`：行签名（每行 RGB 均值）× SAD 匹配算纵向位移，
  重叠不足 / 误差超阈值判为接不上；`append` 只接下一帧新增的底部若干行
- `ScrollingCaptureSession`：定间隔（250ms）抓同一块区域，
  连续 6 拍无位移自动收工，也可「完成 / Esc」结束，高度上限 20000px
- `CaptureEngine.captureRegion`：用 `configuration.sourceRect` 只抓选区（不再整屏抓再裁），
  且把本 App 排除出画面——所以控制条压着选区也不会被拍进去
- `OverlayCoordinator.purpose = .regionPick`：复用同一套选区 UI，把矩形交回来而不是截图
- `ScrollingCapturePanelController`：贴着选区下方浮出的控制条（进度 / 完成 / 取消）
- 入口：菜单「滚动长图…」+ 热键 `⌘⇧L`（`HotkeyAction` 第五个动作）

### 热键默认不设 + 选区去掉放大镜（本轮调整）
- `HotkeyAction` 去掉 `defaultHotkey`，`Hotkey` 去掉那五个默认组合键常量：
  五个动作出厂都不绑定快捷键，避免抢占系统 / 其它 App 的组合键
- `SettingsStore.setHotkey(_:for:)` 支持传 nil 清除；`hotkey(for:)` 返回 `Hotkey?`
  （旧版单条 `hotkey.areaCapture` 仍会迁移过来）
- `AppDelegate.applyHotkey(_:for:)` 改为接受 `Hotkey?`：nil 即注销该动作的键；
  撞车 / 被占用时仍回滚到上一个可用组合键
- 设置页每行加「清除」按钮，未设置时显示「未设置」
- 区域截图选区**去掉 8x 放大镜与取色读数**（`OverlayCanvasView` 删掉 8 个放大镜图层
  与 `updateMagnifier*`，`Theme.magnifierSize/magnifierZoom` 一并移除），
  保留十字准线与尺寸标签；滚动长图起步的选区也复用这套精简交互

### UI 重构：对齐 Tinycast 组件风格（本轮新增）
- 规范文档 `.specstory/history/docs/20260916_UI设计规范.md`：移植 Tinycast 的五条承重规则
  （磨砂 + scrim 表面 / 一条 alpha ramp / 浮动条不是 chrome / 边缘溶解 / 玻璃只给浮动控件）
  与全部令牌、组件规范、界面映射
- 设计系统 `Jietu/UI/DesignSystem/`（`Theme.swift` 重写）：
  - `Theme.Colors`：`adaptive(dark:light:)` + `ramp(dark:light:)` 墨色 ramp
    （panelScrim / selection / rowHover / controlSurface / border / textPrimary…）、
    功能色（brand 保留 Jietu 蓝、selectionGreen、destructive/success/warning/progress）
  - `Theme.Spacing` / `Radius` / `Size` / `Typography` / `Duration`；
    保留 `brand` / `selectionGreen` / 遮罩功能参数等兼容别名供 AppKit 画布继续读
  - 新组件：`VisualEffectView`、`FloatingSurface`、`BarButton` / `BarIconButton`、
    `KeyCapChip`、`GlassButton` / `GlassCircleButton`、`SettingsRow` / `SettingsCard` /
    `SettingsSectionHeader` / `SettingsIcon` / `settingsEnabled(_:)`、`View.frosted(in:)`
  - **图标主题色**：`Theme.Colors.accent = Color.accentColor`（= `AccentColor` 资产 = 品牌蓝），
    图标统一取 `Theme.Colors.icon`（同一处来源、同一个颜色，不逐分区换色）；
    开关打开态 / 分段 / 单选 / 聚焦环由 `.tint(Theme.Colors.accent)` 统一；
    行组件升级为 `SettingsToggleRow` / `SettingsControlRow`（都带图标槽）；
    `HotkeyAction.symbol` 补每个动作的图标
  - **快捷键录制器改成一个自包含组件**：固定 120×24 槽，未绑定显示「未设置」、
    录制中「请按组合键…」、已绑定逐键 keycap 且**悬停右侧浮出 ✕ 清除**——
    删掉原来单独的「清除」文字按钮（对齐参考项目的 `ShortcutRecorder`）；
    `Hotkey.keycaps` 拆出逐个按键芯片
  - 删除锁深色的 `VisualEffectBackground`，改为不锁外观的 `VisualEffectView`
- 重构的界面（全部 SwiftUI 内容界面）：
  - **设置页**：侧栏 `List(.sidebar)` + `Form(.grouped)` 分组卡片；行改 `SettingsRow`
    （图标 + 标题 + 副标题 + 尾部控件，开关/下拉也带副标题）；窗口 660×470 → 820×600
  - **快捷键录制**：`SettingsRow` + `KeyCapChip`（未设置显示「未设置」，录制中品牌色）
  - **权限引导**：`dialogIcon` 品牌字形 + `SettingsCard` 状态卡 + `GlassButton` 操作
  - **QAO 浮窗**：`panelScrim` + 磨砂 + `Radius.menuPanel`；四角 / 中央按钮走玻璃组件
  - **原地标注工具栏 + 选项条**：`FloatingSurface` + `BarButton`（选中 `controlSurface`）
  - **标注编辑器工具栏**：同样走 `BarButton` 家族，快捷键与 tooltip 直接落在 Button 本体
  - **历史面板**：`FloatingSurface` + 玻璃「完成」；面板尺寸改为读 token（340×560 对齐）
  - **滚动长图控制条**：`FloatingSurface` + 玻璃按钮，高度按内容重算（320×124，不再裁剪）
  - QAO / 滚动条面板去掉强制 `.darkAqua`，表面明暗交给 ramp 跟随系统
- 测试：修复 `SettingsStoreTests.recentCaptures`——`recentCaptureURLs` 现在会过滤不存在的文件，
  用例改用真实临时文件

### Debug 独立渠道（dev）（本轮新增）
- Debug 配置对齐参考项目：`PRODUCT_NAME = "Jietu Dev"`、
  `PRODUCT_BUNDLE_IDENTIFIER = com.ixxxxoooo.jietu.dev`、`PRODUCT_MODULE_NAME = Jietu`
  （固定，否则 `@testable import Jietu` 会断）；`JietuTests` 的 `TEST_HOST` 同步指向
  `Jietu Dev.app/Contents/MacOS/Jietu Dev`
- 独立 bundle id = 独立 TCC 授权 / 偏好 / 登录项，本地调试不再污染正式版
- `Config/Info.plist` 的 `CFBundleDisplayName` 改为 `$(PRODUCT_NAME)`，系统设置的
  「屏幕录制」列表里 Debug 显示为 **Jietu Dev**
- 新增 `AppIdentity`（`Core/AppIdentity.swift`）：`displayName` / `isDevChannel` /
  `bundleIdentifier`；设置页「权限 › 授权对象」与权限引导都显示当前身份，
  dev 渠道额外带 `accent` 色的 **dev** 药丸；权限引导标题 / 步骤 / 重启按钮改用显示名
- `AGENTS.md` 补构建渠道表与新的产物路径

### 设置窗口 chrome / 材质 / 外观 / 关于（本轮新增）
- **窗口 chrome 对齐参考项目**（`SettingsWindowController` + `SettingsToolbarController` +
  `WindowChrome`）：
  - styleMask 补 `.miniaturizable` / `.resizable`——此前黄绿两个红绿灯是灰的、点不动
  - 设置窗改成唯一保留**系统标题栏**的窗口：`titlebarAppearsTransparent = false` +
    `titleVisibility = .visible` + `toolbarStyle = .unified` + `titlebarSeparatorStyle = .none`
  - AppKit `NSToolbar`（不是 SwiftUI `.toolbar`）放分区标题，`window.title` 同步当前分区
- **侧栏材质修正**：改用 `NSSplitViewController(sidebarWithViewController:)`
  （固定 215、不可折叠、两列 `.scrollContentBackground(.hidden)`）——
  SwiftUI `NavigationSplitView` 拿不到系统 sidebar 材质，这就是「材质不对」的原因
- **分区标题 + 回退 / 前进**：工具栏左侧两个 `isNavigational` 的 `NSButton`
  （`autovalidates = false`，可用性来自 `SettingsNavigationState` 的历史）；
  新增 `SettingsNavigationState`（当前分区 + 回退 / 前进栈，随窗口释放）
- **设置行改回 stock 控件**：`Toggle` / `Picker` / `LabeledContent` 两段式 label
  （副标题的字号 / 颜色 / 换行由系统给），只有尾部是自定义控件的行才用 `SettingsRow`；
  删掉上一轮自造的 `SettingsToggleRow` / `SettingsControlRow` / `SettingsIcon` 与行内图标，
  设置窗全局 `.tint(Theme.Colors.accent)`
- **新增「外观」设置**：`AppAppearance`（跟随系统 / 浅色 / 深色），
  `SettingsStore.appearance` 持久化，启动时与变更时 `NSApp.appearance` 全局应用
- **新增「关于」页**：hero（App 图标 88 + 名称 + 版本药丸 + 一句话）+
  信息行（构建渠道 + dev 药丸 / Bundle ID / 屏幕录制权限）+ Form 外的底部版权
- **QAO 浮窗悬停去掉品牌色描边**：改回 ramp 细边（`border`），主表面材质交给系统
- **侧栏选中图标修正**：图标只在未选中时给 `icon` 色，选中态交回系统上色——
  选中底色有「有焦点=强调蓝 / 无焦点=浅灰」两种，写死颜色会在其中一种下看不见
- 规范文档补 4.0 / 4.0.1 / 4.1 / 4.2 四节（窗口 chrome 与行写法是最容易做错的两处）

### 仓库身份与剪贴板（本轮调整）
- bundle id / 日志 subsystem 里的用户名改为 GitHub 用户名 `ixxxxoooo`：
  正式 `com.ixxxxoooo.jietu`、Debug `com.ixxxxoooo.jietu.dev`、测试 `com.ixxxxoooo.jietu.tests`；
  仓库内不再出现本机用户名，`AGENTS.md` 的重启命令改为仓库根目录下的相对路径
- 「关于」页新增「链接」卡片（GitHub 仓库 / 问题反馈），排版照参考项目的 AboutLinkRow
- **剪贴板修正**：`editorMode` 默认是「原地编辑」，这条路径此前绕过了写剪贴板，
  所以截完粘不出来；现在 `handleCaptured` 的原地分支在打开编辑器前先写一次剪贴板，
  编辑器点 ✓ 再写一次（覆盖成带标注的成图）。所有路径统一走 `copyToClipboard(_:)`

### 权限引导改版（本轮新增）
- 引导页从单屏改成**四步向导**（欢迎 → 屏幕录制权限 → 快捷键 → 完成），排版照参考项目的
  Onboarding：hero（App 图标 / 字形 60pt + `.title2.bold` + `.callout` 副标题）→
  `OnboardingCard` + `OnboardingRow` + `OnboardingDivider` → 底部圆点 + 上一步 / 稍后 / 主按钮
- 新增 `OnboardingCard.swift`（卡片 / 行 / 分隔线 / 状态药丸），删除 `SettingsCard`
  （引导页有自己的一套，设置面板用 stock `Form`）
- 窗口高度随步骤自适应：视图 `onGeometryChange` 量理想高度 → `OnboardingWindowController.fit(height:)`；
  窗口改透明标题栏 + `isMovableByWindowBackground`，顶部加 `sheen → clear` 渐变
- 引导页里可直接设「截图后自动复制到剪贴板」「登录时启动」与区域截图快捷键
  （写回同一个 `SettingsStore`）；`HotkeyRecorderView` 拆出可复用的 `HotkeyRecorderChip`
- hero 每步一个字形（权限=取景框、快捷键=键盘、完成=对勾），实测底部内边距 20pt 与右侧对称

### 权限检测修正（本轮调整）
- 根因：`AppDelegate.hasUsableScreenCapturePermission` 是**启动时的快照**，
  用户运行中授权后就一直判为「没权限」；引导页虽有 1 秒轮询，但入口卡在快照上
- 改为参考项目那套「现读活值」：`ScreenCapturePermission.isGranted` 每次现读，
  截图入口统一走 `requireScreenCapturePermission()`，不可用就打开引导页
- 新增 `wasGrantedAtLaunch`（启动最前面 `recordLaunchState()` 记一次）与
  `needsRelaunch`：区分「没授权」与「已授权但要重启」——屏幕录制的授权
  在授权时的那个进程里不生效
- 引导页权限步骤：状态实时显示；新增「重新检测」；「重启 Jietu」在
  `needsRelaunch` 或「本会话引导过授权但状态未变」时出现；模型改为
  1 秒轮询 + `didBecomeActive` 立刻复查
- 设置页权限分区：同样实时（`Timer.publish` + `didBecomeActive`），
  状态行带「重新检测」，需要时多一行「重启 Jietu」
- 截图失败弹窗：`CaptureError.suggestsRelaunch` 决定首个按钮是「重启 Jietu」

### 权限入口收敛为「只有拖拽授权」（本轮调整）
- 去掉两处「重新注册」（菜单栏一项 + 权限引导链接）与 `ScreenCapturePermission.reRegister()`
  /`resetSystemEntry()`：它会清掉当前**已经生效**的授权，代价大于收益
- 设置页权限分区的「打开系统设置 / 授权屏幕录制」两个按钮合并成一个「拖拽授权…」
- 修面板里的「设置」齿轮：`x-apple.systempreferences:` 在**已经停在这一栏**时是 no-op，
  窗口不会前置 → 补 `SystemSettingsWindow.activate()` 显式激活（实测：推到 Finder 后面
  再走一次，系统设置回到前台）
- 面板加穿透兜底：每次 tick 检查，拖拽回调万一没回来（拖到别的 App 上被打断）就复位
  `ignoresMouseEvents`，否则面板会卡成鼠标穿透、里面按钮全点不动

### 权限入口与引导优化（本轮新增）
- **启动不主动弹窗系统设置**：移除了 `AppDelegate.applicationDidFinishLaunching` 启动时直接调用 `ScreenCapturePermission.request()` 的行为；未授权时仅展示引导页，只有用户在引导页中点击「授权屏幕录制」或「去授权」时，才主动调起系统授权和打开系统设置拖拽面板。
- **菜单栏新增辅助功能授权项**：
  - 菜单栏新增「辅助功能权限：已授权/未授权」实时状态项；
  - 菜单栏新增「拖拽授权「辅助功能」…」项，点击直接调起系统辅助功能面板并浮出拖拽卡片；
  - `menuNeedsUpdate` 每次展开菜单均即时刷新屏幕录制与辅助功能的最新授权状态。
- **引导页增设辅助功能（可选）**：在权限引导 Step 1 中增加「辅助功能（可选）」行与「去授权」按钮，说明用于自动滚动长图，提供手动按需授权入口。

### 权限入口补强（历史）
- 菜单栏权限分组「拖拽授权「屏幕录制」…」，底部「重启 Jietu」

### 屏幕录制权限「列表里看不到 App」修正（本轮新增）
- 根因：Debug 构建走了 Xcode 16+ 的 `ENABLE_DEBUG_DYLIB` 默认值——真代码在
  `Jietu Dev.debug.dylib`，主可执行文件只有 58KB 的壳；TCC 靠主可执行文件的签名身份
  登记「屏幕录制」，壳的身份建立不起来，于是出现「API 说已授权、列表里却看不到」的怪状态
- 修复：Debug 配置加 `ENABLE_DEBUG_DYLIB = NO`，主可执行文件恢复完整二进制（~6.5MB）
- 实测：`CGPreflightScreenCaptureAccess() = true`，`--selftest-capture` 能正常落 2.1MB PNG，
  说明授权**一直都在生效**，列表不显示只是界面渲染问题（自签名证书 `Jietu` 未受信任）
- 文案修正：不再让用户「去列表里勾选」（会误导），引导页 / 设置页统一说明
  「用拖拽授权把本 App 拖进列表」
- `AGENTS.md` 补「权限排查（屏幕录制）」一节与 `ENABLE_DEBUG_DYLIB = NO` 的说明

### 拖拽授权（对齐 PermissionFlow）（本轮新增）
- 背景：系统设置「屏幕录制」只列**申请过**的 App，自签名 / Debug 构建经常不出现；
  但那一栏**接受拖入**，把 `.app` 拖进去就等于加入并授权 —— 这是
  [PermissionFlow](https://github.com/jaywcjlove/PermissionFlow) 的核心做法
- 没有引入依赖，按同一机制自己实现（保持设计系统风格，且避开它的辅助功能权限要求）：
  - `SystemSettingsWindow`（`Core/Permissions/`）：用 `CGWindowList` + owner pid 反查
    `com.apple.systempreferences` 拿到系统设置窗口 frame；不依赖 AX，也不需要新权限
  - `PermissionDragPanel`（`UI/Permissions/`）：`.borderless + .nonactivatingPanel`、
    `level = .floating`、不抢焦点；拖拽时 `ignoresMouseEvents = true` 让 drop 穿透；
    `hostingView.sizingOptions = []` 保证 `setFrame` 是几何唯一来源
  - `AppBundleDragCard`：`NSDraggingSource` + Finder 风格多类型 pasteboard 载荷
    （少写一种系统设置就不收）
  - `PermissionDragController.shared`：打开面板 → 0.4s 轮询跟随系统设置窗口
    （对齐右侧内容区、让开 230pt 侧栏）→ 窗口关掉自动收工；面板里有「重启 Jietu」
- 面板外观按参考项目的**浮动面板玻璃**做：新增 `View.glassPanel(cornerRadius:)`
  （静态 `.regular` + `Radius.menuPanel`，不描边），与 `PopoverMenu` / `NoteSwitcherView` /
  `ExtensionActionsPanel` 同配方；`frosted(in:)` 那套（interactive + tint）仍然只给控件。
  面板内：卡片是 `controlSurface` 的槽、头部两个按钮 `BarIconButton`、底部 `GlassButton`
- 入口：菜单栏「拖拽授权「屏幕录制」…」、设置页权限分区「授权屏幕录制」、
  权限引导权限步骤的主按钮
- 实测定位正确：系统设置 cg `(228,363,740,748)` → 面板 AppKit `(458,203,510,126)`，
  即 `x = 228+230`、`y = 329-126`，与算式一致

### 选区定稿后仍可拖边缘改区域（本轮新增）
- 原地标注态下，选区绿框与控制点**不再隐藏**：工具栏弹出后抓着边缘就能继续调区域
  （此前一进标注态就把绿框藏了，选区就再也改不动）
- 鼠标分流：标注态里命中选区 handle 优先接管拖拽，其余照旧进入标注；
  拖拽结束回到 `.settled`，不算一次标注
- 改区域时把标注 / 橡皮笔迹按**新原点**平移（`SelectionGeometry.imageDelta`），
  让它们继续钉在画面同一处；同时重建预览底图、重画标注层、跟随工具栏位置
- 撤销栈快照补上 `selection`：改完区域再撤销会连选区一起回滚，标注不会错位
- 新增单测钉住平移不变式（`imageDelta` 的符号与「同一画面点在新旧选区里的图像坐标之差」）

### 钉图支持右上角关闭按钮与 ⌘W 快速关闭（本轮完善）
- **右上角关闭按钮**：符合 UI 设计规范，复用 `GlassCircleButton`（磨砂圆形玻璃按钮），
  悬停显隐，带 `关闭 (⌘W)` tooltip；`hitTest` 与 `updateCursor` 优先响应关闭按钮，不被右上角手柄劫持
- **修复关闭按钮需点两次才生效（First Mouse 首击穿透）**：
  - 此前 `NSHostingView` 默认 `acceptsFirstMouse` 为 `false`，后台/未激活窗口首击被系统吞掉用于激活窗口，必须点第二次才触发
  - 实现 `PinCloseHostingView` 与 `PinFirstMouseButton`：重写 `acceptsFirstMouse(for:) -> true`，后台状态也能单次点击即触发关闭
  - `PinCloseHostingView` 自身实现 `mouseDown`/`mouseUp`，并在 `PinContentView.mouseDown` 增加命中双重保底，彻底杜绝首次点击吞事件
  - `GlassCircleButton` 增加 `.contentShape(Circle())`，保证 28pt 整个圆形区域内任意像素均能灵敏响应点击
- **修复 ⌘W / Esc 无法关闭**：
  - `PinPanel` 去除 `styleMask` 中的 `.nonactivatingPanel`（此前它导致窗口无法激活应用与获取键盘焦点）
  - `pin(...)` 与 `mouseDown` 时显式激活应用（`NSApp.activate`）并将窗口设为 key window
  - `PinPanel` 增加 `performKeyEquivalent` / `performClose` / `cancelOperation` 兜底关闭
  - 多张钉图共存时按 ⌘W / Esc 逐张关闭当前激活的钉图，关闭后焦点自动顺延至上一张
- **新增单测** `PinWindowTests`（共 7 个用例）：覆盖 KeyWindow 资质、⌘W / Esc 响应、兜底派发、右上角 HitTest 优先级、首击响应（acceptsFirstMouse）与单次点击立即触发关闭

### 浮窗图标与钉图统一成同一套玻璃图标按钮（本轮）
- **抽出公用组件** `GlassControlButton`（`UI/DesignSystem/`）：钉图那两个圆钮与浮窗的五个图标
  现在是**同一个 NSControl**——同款系统玻璃 + `glassFrost` 色调、悬停 `menuHover` 微光（0.12s）、
  按下**当场**缩放 0.85 + 压暗（0.08s）、`acceptsFirstMouse` 首击即生效；
  圆盘外形给四角与钉图用，胶囊外形（图标 + 文字）给浮窗中央的「保存」用
- **浮窗图标层换成 AppKit**（`QuickAccessControlsView` + `NSViewRepresentable`）：
  直径 30 → **28**（与钉图对齐）、补上按下反馈与同款柔和投影；几何改成纯函数
  `frame(of:in:)`，自检与单测按**同一套**算注入点，不再各抄一遍边距；
  卡片比控件还小时把按钮夹回卡片内（超宽截图换算出来只有十几点高）
- **修掉「浮窗五个图标全点空」的根因**：`GlassControlButton.hitTest` 把**父视图坐标系**的点
  当成自己的 `bounds` 判——不靠原点的按钮永远命不中（钉图那两个是宿主按 frame 兜住的，
  浮窗没有这层兜底，于是点击全落到下层 SwiftUI 上）。现在按 `convert(point, from: superview)` 转换
- **灵敏度实测**（新增 `--selftest-quickaccess`：真弹浮窗、逐图标注入真实点击）
  - 按下→反馈 5~25ms、抬起→动作 ≤ 9ms（应用内计时，含系统投递）
  - 动作→可见效果：落盘 1ms、写剪贴板 0ms、钉图窗口出现 9ms、编辑器出现 115ms
  - 关闭：抬起→开始侧滑 46ms、面板消失 273ms（0.24s 侧滑动画）
  - **光标甩过去就点**（不等图标淡入）：关掉了——不点空、不误触发「标注」
  - 量测口径：注入事件的投递抖动（实测 1~90ms）是 CGEvent 管线的账，不算界面延迟，
    所以「动作→效果」一律用应用内时钟从**动作发生那一刻**起算；控件内部延迟单独记
    （`InteractionMetrics`，DEBUG）
- **新增单测** `QuickAccessTests`（8 个用例）：几何贴边与两两不重叠、超扁卡片夹回卡片内、
  hitTest 的坐标空间、静息态不吃事件、按下当场反馈而动作在抬起、拖出按钮不触发、胶囊真的带文字

### 默认值与措辞调整（本轮调整）
- 预览浮窗默认值：停靠位置 `右下角` → **左下角**（`QuickAccessPosition` 默认 `.bottomLeft`）；
  自动关闭 `3 秒` → **30 秒**（`SettingsStore` 默认 30，单测同步）
- 「就地编辑」全项目改为「**原地编辑**」（13 个文件：源码 / 测试 / README / 规范 / 进度）
- 拖拽授权面板去掉「设置」齿轮按钮：它在系统设置已关闭时拉不起来，
  误导大于收益。改为 `present()` 里 `openSystemSettings()` 之后补一次
  `SystemSettingsWindow.activate()`（系统设置已开着时也能拉到前台）；
  系统设置关掉后面板会在 ~3 秒内自动收工，不会留一个孤零零的浮窗

### 截图浮窗（QAO）按「截图为主体」重做（本轮调整）
- 去掉固定卡片：`quickAccessCard`(236×152) → `quickAccessCardMax`(260×180)，
  卡片尺寸按截图**原始宽高比**等比算（`QuickAccessView.panelSize(for:)`），永不拉伸 / 不 letterbox
- 面板 frame 跟卡片一致，叠放时每张按**自己的高度**占位（`Entry.size`），尺寸不同也不重叠
- 静息态只有截图，不显示任何按钮；悬停时截图 `blur(16)` 露出底下的毛玻璃
  （不再用手搓黑/白压色，修掉浅色外观下按钮变黑压暗底的对比度问题），操作层浮在截图之上
- 底色改用 `glassPanel`（浮动面板玻璃，与拖拽授权面板同配方）；新增极细内描边
  （`border`/`cardStroke` 上亮下淡渐变 = 轻量微光）
- 阴影改为**系统窗口阴影**：`NSPanel.hasShadow = true` + 进场结束后 `invalidateShadow()`；
  删掉原来的 SwiftUI `.shadow` 与 14pt 阴影留白（那圈留白+阴影就是「丑」的来源）
- 实测：1600×600→260×98、1000×800→225×180、600×1200→90×180（比例 2.65 / 1.25 / 0.50 全对），
  三张各自圆角无白卡、静息无按钮、悬停那张模糊+按钮、未悬停两张前后像素一致、阴影贴合无光晕

### 窗口定位与单实例（本轮调整）
- **钉图默认钉在屏幕正中**（原来钉在右上角）；多张仍按 24pt 逐张错开避免完全重叠
- **标注编辑器打开即居中**：不再用 `window.center()`（它按窗口**当时的** frame 居中，
  而 SwiftUI 首帧布局会改一次尺寸，结果偏掉）。改为按内容尺寸显式算 frame 居中，
  并在上屏后再校一次；`NSHostingView.sizingOptions = []` 让窗口尺寸由控制器说了算
- **单实例**（原来 `relaunchApp()` 用 `open -n`，那正是多实例来源）：
  - `relaunchApp()` 改成后台 shell「等自己退出后再 `open`」（不带 `-n`，走 LaunchServices）
  - 启动加守卫 `handOffToExistingInstance()`：已有同 bundle id 实例就把前台交给它再退出
  - 刻意**不用** `LSMultipleInstancesProhibited`（声明式禁止多开没法绕过，会让
    `xcodebuild test` 的测试宿主起不来）；守卫在 XCTest 宿主里跳过
  - 实测四种路径都只剩 1 个实例：`open` 两次 / `open -n` / 直接跑二进制 / 重启流程

### 工具栏与文字输入调整（本轮调整）
- **去掉放大镜工具**（工具枚举 / 渲染 / 样式 / 存档默认值 / 单测共 7 个文件）；
  旧存档里存着 `magnifier` 的会回落到默认工具
- **浮窗边框**改淡改细：半个点宽 + 45%/55% 透明度渐变
- **颜色圆点**在浮窗与编辑器工具栏都从 20 缩到 14
- **浮动子工具栏（颜色 / 粗细）居中到被点的按钮下方**：按钮把 `midX` 报给 model
  （`OptionsAnchorReporter` + 工具条坐标空间），AppKit 据此定位并夹进屏幕；
  原来的「靠右对齐到主栏右边缘」换成「对齐点击位置」
- **就地文字输入改为所见即所得**：点击处原地出现输入框，字体用渲染器同一套（Helvetica）、
  字号 = 写入字号 ÷ 缩放、颜色 = 标注色；**不再铺黑底 / 白字 / 系统边框**，
  只留一圈同色细边框；框宽贴文字宽度；浮窗用 `NSTextField`，编辑器用 SwiftUI `TextField`
- **编辑器工具栏**：去掉「关闭」与「适应」，「原始」→ 刷新图标（`arrow.counterclockwise`），
  缩放组紧跟颜色 / 粗细（去掉中间那一大片空），`BarButton` 加 `.focusEffectDisabled()`
  去掉首个按钮的系统蓝焦点环；顺手删掉不再使用的 `zoomToFit()`
- 实测：工具条工具顺序 = 选择/矩形/椭圆/箭头/直线/画笔/高亮/马赛克/模糊/文字/序号/橡皮；
  文字框 `font=Helvetica@21`（lineWidth 7 → 42px ÷ scale 2）、`textColor=标注色`、
  `bordered=false`、`drawsBackground=false`、`borderWidth=1`、左上角正好落在点击处

### 去掉选中标注的浮动操作列（本轮调整）
- 编辑器里选中一条标注后右侧那排 6 个圆形按钮（置顶 / 置底 / 复制一份 / 复制样式 /
  粘贴样式 / 删除）整列删除
- **功能没丢**：这些操作的快捷键改用隐藏按钮承载（`selectionShortcuts`：
  0 尺寸 + 透明 + `accessibilityHidden`，和参考项目挂 ⌘F 的做法一致）
- 删除用工具栏的 ⌫（本来就绑在工具栏那支删除按钮上）

### 编辑器子工具栏改成悬浮（本轮调整）
- 颜色 / 粗细子工具栏原来插在工具栏下面的 `VStack` 里，一展开就把画布挤下去
- 改成 **overlay 悬浮**：`anchorPreference(OptionsAnchorKey)` 记录颜色 / 粗细按钮的 bounds，
  `.overlayPreferenceValue` 画成 overlay（不参与布局），`optionsBarCenterX/Y` 把它
  **居中到被点的按钮下方**并夹进窗口；原地模式（工具栏在底部）改贴按钮上方
- 实测：点击前后两帧只差在「工具栏 + 其下方一条带」，**画布区域零变化**（没有被挤动）；
  子工具栏（7 个色点）居中在点击处（变化带中心 553pt vs 点击 563pt），无裁切

### 滚动长图：参考 capcap 业界最佳实践重构（本轮优化）
- **核心算法升级**：全面参考 **realskyrin/capcap** 的成熟实现重构长截图链路：
  - **Apple Vision 2D 平移配准**：引入 `VNTranslationalImageRegistrationRequest` 替代传统 1D 行平均签名互相关，彻底根治大面积空白、代码段、正文等低纹理/相似行导致的位移误判与跳变；保留行签名作为低开销兜底。
  - **吸顶元素（Sticky Header）动态检测与剔除**：在两帧之间扫描顶部完全静止的行并在送入 Vision 前切除，避免网页顶部固定导航栏（GitHub / 文档站等）干扰位移计算，同时确保吸顶栏只在长图顶部出现一次。
  - **滚动条干扰切除**：扫描右侧边界变化列，动态切除滚动条滑块区域，防止滑块移动污染配准。
  - **平滑滚动沉降检测（Settled Frame Polling）**：轮询对比连续两帧 raw pixel 数据（`CFEqual`，15~80ms 退避），确保浏览器/系统惯性与平滑滚动完全绘制沉降后再抓帧，杜绝模糊过渡态。
  - **单趟高速内存拼接与轻量预览**：引入 `BitmapData` 基于 `memcpy` 的行切片拷贝机制，替代原有每拍重建 `CGContext` 重绘整图的 $O(N^2)$ 内存与 CPU 浪费；收工时单趟开辟最终尺寸直接组装。
  - **ScreenCaptureKit 单会话复用**：`CaptureEngine` 新增 `RegionCapturer`，会话初始化时只查询一次 `SCShareableContent` 并复用 Filter，单帧截取耗时从 ~80ms 降至 ~10ms。
- **单测覆盖**：
  - 新增 `stitchesWithStickyHeader`（验证 25px 吸顶导航栏与滚动正文的 2D 配准与拼接）
  - 新增 `bitmapNearlyIdenticalCheck`（稀疏采样快速判重）
  - 全套 73 个单元测试全部通过。

### 滚动长图：自动滚动（历史）
- 参考项目确认是 **realskyrin/capcap**（`Capture/AutoScroller.swift` + `EditWindowController.startAutoScroll`）
- 新增 `AutoScroller`（`Core/Scrolling/`）：
  - 往选区中央发**合成像素滚动**（`CGEvent(scrollWheelEvent2Source:units:.pixel, wheel1: -step)`）
  - 步长 = 选区内高的 **15%**，夹在 **60–180pt**（capcap 注释：更小步长留 ~85% 重叠，配准才稳）
  - 装 session 级 **event tap**：选区内的用户鼠标 / 滚轮输入丢掉（自己的合成事件带 tag 放行），
    **任意按键即结束**并吞掉该键；tap 被系统临时禁用会自动重开
  - 合成事件本身也需要授权，否则 `CGEvent.post` 静默失效
- 新增 `AccessibilityPermission`（申请 / 现读状态 / 打开系统设置）与 `PermissionPane`
  （把「拖拽授权」面板参数化，同时服务屏幕录制与辅助功能两栏）
- 会话：`ScrollingCaptureSession` 加 `mode`（automatic / manual）与
  `regionInGlobalCGPoints`；自动模式先发一步再等画面稳定后抓帧；
  停止阈值自动 **4 拍**、手动 6 拍
- 入口：菜单拆成「滚动长图（自动滚动）…」与「滚动长图（手动滚动）…」，**热键走自动**；
  自动模式缺辅助功能权限时：申请 + 弹窗让用户选「改用手动滚动 / 打开系统设置」（不阻塞）
- 设置页权限分区拆成「屏幕录制」与「辅助功能」两段，各自有状态、重新检测、拖拽授权
- 控制条文案按模式区分：自动「正在自动滚动…任意键停止」/ 手动「匀速滚动，停止约 1.5 秒自动完成」
- 实测：把系统设置当滚动目标跑自动模式 → 步长 90pt（600pt×15%）、
  420pt 选区拼出 **760×1163**（原视口 840px，`grew=true`），自动滚动与拼接都生效
- 单测：新增 `AutoScrollTests`（步长 15% 与 60–180 夹取）

### 滚动长图梳理（历史）
- 实现是完整的：菜单「滚动长图…」/ 热键第五个动作 → 复用遮罩框选（`OverlayCoordinator.purpose = .regionPick`）
  → `ScrollingCaptureSession` 每 0.25 秒抓同一块区域（`CaptureEngine.captureRegion` 用
  `SCStreamConfiguration.sourceRect`，并把本 App 排除出画面）→ `ScrollStitcher` 行签名互相关算位移、
  只接新增行 → 停止滚动 6 拍（1.5 秒）自动收工，或「完成 / Esc」→ 结果走正常交付（QAO）
- 单测补强：原来只抽查 5 行 + 固定 10px 位移；现在加了「位移不均的多帧拼接逐行核对」
  「没滚动的帧不会长高」「滚太快无重叠时跳过该帧」
- 文档：`开发进度.md` 里「待完善」原先还写着「滚动长图尚未实现」，与上面已完成的记录矛盾，已修
- 已知代价：`append` 每接一帧都会把整张长图重画一次 → 累计 O(n²) 的像素拷贝；
  20000px 上限内可用，但滚动很久时会有卡顿，可按需改成「一次开大 context、只往新行画」

### 措辞调整
- 设置页里「预览浮窗」的菜单标题（侧栏项 + 分组标题 + 窗口标题）改成「浮窗」；
  权限引导里那句也一并改

### 滚动长图：框选后先选「自动截图 / 开始截图」（本轮调整）
- 原来框选完**立刻开始采样**（自动模式还会马上开始滚），没有任何「开始」的入口
- 改成控制条先停在**待开始**态：`[取消] [开始截图] [自动截图]` + 状态「框选完成」；
  点「自动截图」由 `AutoScroller` 自己滚，点「开始截图」把鼠标放进选区自己滚
- 进行中态仍是 `[取消] [完成]` + 「已拼接 N px」与按模式区分的提示文案
- 菜单恢复成单入口「滚动长图…」（模式改在控制条上选），热键同样
- 控制条 320×124 → 330×136、提示允许两行（原来待开始的说明会被截断）
- 会话改为**按需创建**：点按钮才建 `ScrollingCaptureSession` 并开跑

### 构建/重启流程固化成脚本（本轮）
- 新增 `Scripts/restart-dev.sh`：**构建 → 杀旧实例 → 启动 → 校验跑的是最新产物**
  - 关键校验：比较「进程启动时刻」与「产物 mtime」；发现跑旧了自动再重启一次
    （`xcodebuild` 尤其 `test` 会刷新 mtime，正在跑的进程可能还是上一次构建的）
  - 打印：实例数 / 产物 mtime / 进程启动时刻 / 进程路径 —— 改完代码就看这几行
  - `ps lstart` 会随系统语言本地化，脚本里用 `LC_ALL=C` 才能被 `date` 解析
- `AGENTS.md` 的「修改后必做三步」改成直接调这个脚本

### 修「滚动长图面板有时候弹、有时候不弹」（本轮）
- 根因：`OverlayCoordinator.purpose` 只在**成功选区**（`startScrollingCapture`）与**异常**分支重置；
  滚动长图那次选区**被取消**时会把 `.regionPick` 留在原地 → 下一次**普通区域截图**也走
  「只要一个矩形」那条路 → 弹出滚动长图的「自动截图」面板（于是表现为「有时候弹」）；
  等某次滚动长图真的选完区（那里会复位），普通截图又恢复正常（于是「现在又不弹了」）
- 修复：
  - `OverlayCoordinator.finish()` 里 `defer { purpose = .screenshot }`：**一次遮罩会话用完即复位**
    （不变式收到协调器自己身上，调用方不必记着清）
  - `overlays.onFinish` 也显式复位一次；`handleAreaCapture` 进门再复位一次（双保险）
  - 滚动长图入口先收掉上一次「停在待开始」的残留控制条（否则新面板会盖住它、旧的留在屏幕上）
  - 被忽略的情况（遮罩还在 / 会话在跑）写日志，便于排查「点了没反应」
  - Esc 在「待开始」也能取消（原来只 stop 会话，那时没有会话就什么都不做）

### 滚动长图：取景框 + 右侧实时预览（本轮新增，参考 capcap）
- **遮罩在滚动期间保持显示**（原来框选完就整片关掉，用户看不到自己滚到哪了）：
  - 协调器 `commit` 的 `.regionPick` 分支不再 `finish()`，改为 `holdForScrollCapture()`：
    所有遮罩切到「取景框」外观 + `window.ignoresMouseEvents = true`（滚轮落到下面的页面）
  - `OverlayCanvasView.setScrollCaptureChrome(_:)`：**收起冻结图**（露出实时画面）、标注层 /
    控制点 / 十字线 / 尺寸标签 / 提示条全部收起，**保留压暗层与选区绿框**
  - 结束后 `OverlayCoordinator.releaseScrollChrome()` 才真正关掉遮罩
- **右侧实时预览** `ScrollingPreviewPanel`（照 capcap 的 `ScrollPreviewWindow`）：
  124×408 无边框不激活面板、**鼠标穿透**、层级 `screenSaver + 2`（高于遮罩，否则被压暗）；
  贴在选区右侧 12pt、顶部对齐，右边放不下自动翻到左侧；缩略图先降采样再交给 SwiftUI
  （不然每 0.25 秒把两万像素高的图塞进去会很卡）
- 会话新增 `onPreview`：每拼上新内容回一次长图（首帧也给），驱动预览
- 控制条层级 `.floating` → `.screenSaver + 1`（否则被遮罩压住看不见）
- 修「单击整窗吸附」时绿框不出现：那条路径原来画完选区就直接 `commit()`，
  从没调过 `updateSelectionLayers()` —— 遮罩以前随即关闭所以看不出来，现在留着就露馅了
- 实测（合成点击提交选区）：`OverlayWindow ignoresMouse=true` 仍可见、
  预览窗 level 1002 在 (1312,612) 顶部对齐选区、绿框像素范围 x 84..824 / y 230..979
  **精确等于选区 740×748@(84,231)**

### 滚动长图：鼠标一停就出「手动 / 自动」模式条（本轮）
- 问题：框选完**松开鼠标并不会弹工具栏**——普通区域截图的拖拽路径本来就不提交
  （只有单击整窗 / ↵ / 双击才 `commit()`），所以滚动长图那边得按 ↵ 才能看到控制条；
  而且控制条是「待开始」卡片（标题 + 两行提示 + 取消 / 开始截图 / 自动截图），尺寸也不小，
  选区靠下时它还会翻到选框**上方**、甚至顶出屏幕
- 改成：**鼠标在选区上停住（0.35s，不必松手）就把模式条浮到选框正下方**
  - `OverlayCanvasView.isRegionPickMode`：拖拽中重新计时，停住即 `onSelectionPaused`；
    松手（`mouseUp`）/ ↵ 立刻回报一次，兜住「拖完马上松手」的快手
  - 选区在这期间**照旧可拖 / 可缩放 / 可移动**：`onSelectionChanged` 让模式条一路贴着走；
    选区被清掉就把模式条收掉。点「手动 / 自动」才真正提交选区
  - 单击整窗那条路径在 `regionPick` 下不再 `commit()`，改为落到「已定」+ 回报（原来会把
    遮罩留在 `pressing` 状态，下一次拖拽会从旧锚点重来）
- 控制条拆两态（`Theme.Size.scrollingModeBar` 252×46 / `scrollingPanel` 330×136）：
  - 待开始 = 一条紧凑窄条 `[滚动图标] 手动 · 自动 · ✕`（工具提示说明谁来滚）
  - 进行中 = 原来的进度卡（已拼接 N px + 完成 / 取消），切阶段时按锚点重排尺寸
  - `present(near:)` / `move(near:)`：优先贴选框**下方**，下方实在放不下才翻到上方，
    两种情况都夹在屏幕可视区内（原来「上方」那条路没有夹，会顶出屏幕）
- `OverlayCoordinator`：`purpose == .regionPick` 不再在 `commit()` 里立刻切取景框
  （用户还在选），改由外部在点完模式后调 `beginScrollCaptureChrome()`；
  回调换成 `onSelectionPaused` / `onSelectionChanged`（`onRegionPicked` 移除）
- 取消路径补全：模式条挂着时 Esc / 右键 / ✕ 都要连遮罩一起收掉
  （`finishScrollingCapture` 里 `releaseScrollChrome()` + `cancel()` 双保险）
- 实测（CGEvent 合成拖拽，选区 1900×900）：停 0.12s 时无条 → 0.7s 后模式条出现在
  选框正下方；继续把选区拉大，模式条跟着走；松手后仍留在选框下方

### 修「点手动/自动都黑屏闪一下 + 没有捕获到滚动内容」（本轮）
- 复现：日志里 `no scrolling content captured` 前只有 `auto scroll step=Npt`，
  0.28 秒就收工；合成拖拽复现 + 三条自检命令逐个证伪/证实，最后定位到两个独立问题：
- **① 取景框其实是黑屏**：`OverlayCanvasView.setScrollCaptureChrome` 收起冻结图后，
  窗口本身还是 `isOpaque = true` + 黑底 → 整屏变黑（用户看到的「黑屏闪一下」）。
  手动模式在这种状态根本没法看着页面滚。
  修：`OverlayWindowController.enterScrollCaptureChrome()` 里把窗口改成
  `isOpaque = false` + `backgroundColor = .clear`，冻结图一收就露出真实页面，
  压暗层照旧只压选区外 —— 这才是取景框。自检证据：同一块区域「含本 App」抓图
  平均亮度 **0.006（全黑）→ 0.859**，「排除本 App」始终 0.97+（抓到的是真实页面）
- **② 自动滚动抢在滚动生效前抓帧**：合成滚轮刚 post 出去，画面还没更新，
  `captureSettledFrame` 就拿到两帧相同、当成「已沉降」把那帧返回 →
  `append` 判「无新内容」；4 拍全空 → 0.3 秒误收工。
  修：自动模式也 `sleep(interval)` 一拍再抓帧（原来手动才有 sleep）
- 顺带：
  - 新增 `startupIntervalsToStop`（起步宽限）：还没拼上任何内容时允许更多空拍
    （手动 20 拍≈5s / 自动 8 拍），免得「刚弹完辅助功能提示就开始倒计时」，
    或用户还没把鼠标挪进选区就失败
  - 失败弹窗文案改成可操作的：说明选区得是「整块能滚动的页面」、以及
    鼠标停在图表 / 下拉菜单这类吃掉滚轮的控件上时自动滚动会带不动
- 自检工具（都在 `#if DEBUG`，`CaptureSelfTest`）：
  - `--selftest-region x,y,w,h [目录]`：同一块区域「SCK 直接抓」vs「整屏抓再裁」，
    顺手**证实了 `SCStreamConfiguration.sourceRect` 的约定：显示器内、原点左上、单位点**
    （主屏与副屏、2x 缩放下两块图内容一致）——「需要手工验证」里那条可以划掉了
  - `--selftest-chrome x,y,w,h [目录]`：遮罩进取景框态后抓「含/不含本 App」两张图，
    用亮度差区分「黑屏」与「透明取景框」；再发一步合成滚动看画面是否随之变化
  - `--selftest-session x,y,w,h [目录]`：不弹遮罩，直接跑真实采样会话（自动模式）
  - `--selftest-frames x,y,w,h [--with-blocker] [目录]`：逐拍 dump 帧 + 每拍的
    平均差 / Vision 位移 / 拼接判定，用来判断「滚动没推动画面」还是「拼接拒了这帧」
  - `--selftest-scrollprobe`：像素/行、continuous、手势 phase、带不带 location
    四种合成滚轮写法轮流试，看哪种能真的推动目标窗口
- 已知边界（不是本轮引入）：自动滚动靠合成滚轮，**目标控件吃掉滚轮就带不动**
  （图表缩放区、下拉菜单、TUI）；这时改用手动，或把选区挪到正常页面区域

### 标注编辑器：最小宽度按工具栏实际所需来定（本轮）
- 问题：工具栏一行有 24 个按钮（13 工具 + 撤销/重做/删除 + 颜色/粗细 + 缩放 4 件 +
  识别文字/复制/保存/钉图），实测**需要 967pt**；而原地编辑窗口的宽度下限写死 620 →
  右边几个按钮（钉图等）直接被窗口裁掉；带标题栏那条路的 `minWindowWidth = 900` 也不够
- 修：
  - `AnnotationEditorView` 抽出单一常量 `toolbarMinWidth`（967，值由 SwiftUI 自己报，
    见下面自检），`minWindowWidth` / `minInlineWidth` 都从它取；
    SwiftUI 那层 `.frame(minWidth:)` 也改成同一常量（原来原地写死 320）
  - `AnnotationEditorWindowController.inlineWindowSize`：图片再窄也 `max(图片宽, minInlineWidth)`；
    多出来的宽度是透明的，只有工具栏那条真的画出来，原地「贴着选区」的观感不变
- 自检 + 回归：
  - `Jietu Dev --selftest-editor <目录> [--window]`：拿 260×180 小图开窗（原地 / 带边框两种
    都行），打印 `NSHostingView.fittingSize`（就是工具栏所需宽度），并落一张截图供目视检查
  - `JietuTests/AnnotationEditorLayoutTests`：把上面那个测量搬成用例，断言
    「工具栏放得进 toolbarMinWidth / minInlineWidth / minWindowWidth」——
    以后谁往工具栏加按钮，这条会先红
  - 实测（截图确认）：原地 260×180、锚点 260 宽时，窗口 967 宽，24 个按钮一行全在，
    最右的钉图不再被裁

### 工具栏布局优化：OCR 移至保存前面（本轮新增）
- **原地截图工具栏**（`InlineMainToolbar`）：将「识别文字（OCR）」按钮移动到「保存」按钮前面，按钮顺序调整为：`[识别文字] [保存] [钉图] [取消] [确认]`；并将原「下载」按钮提示文案统一为「保存」。
- **独立标注编辑器**（`AnnotationEditorView`）：右侧操作区同步调整为：`[复制] [识别文字] [保存] [钉图]`，确保 OCR 紧邻在保存前面。

### 选区放大镜回归：方形像素网格 + 坐标/区域/色值（本轮）
- 之前那版（`4c908ad` 删掉）是**圆形**取景 + 底部一个小药丸读数；这次按参考图重做成
  **方形网格**：光标走到哪跟到哪，一眼能数像素
- `OverlayCanvasView` 新增一组放大镜图层（`Loupe`）：
  - `side = 136`、`cells = 13`（奇数：光标那一格正好在正中），一格 = **图像的一个像素**
  - 图层：图（`contentsRect` 换采样窗口 + `nearest`，不生成中间位图）→ 十字带（行 / 列整条
    淡绿染一下）→ 双层网格（黑 0.28 + 白 0.16，深浅内容上都看得见；路径是常量，只算一次）
    → 中心格白描边（+ 黑垫底，白线落在白色内容上也不丢）→ 圆角外框 + 黑描边阴影
  - 光标贴边时把采样窗口夹回图像内，**十字与中心格跟着光标像素走**（不会错位到窗口中心）
  - 读数面板贴在放大镜下方（放不下翻到上方）：`坐标` = 光标像素（图像像素坐标，原点左上）、
    `区域` = 现在按下去会截到的那块（选框 / 悬停窗口）的像素尺寸、`色值` = #RRGGBB + 色块
  - 取色仍走 `PixelSampler` 且**只在跨过像素时重新取样**（鼠标移动不额外解码大图）
  - 只在「还没定选区」的框选阶段显示：`isSettled` / 原地标注 / 滚动长图取景框 里都自动收起
- 自检 `--selftest-loupe x,y,w,h`：弹遮罩 + 合成「按下 → 拖动 → 停住」，分别抓
  `loupe-dragging-*.png`（放大镜在场）与 `loupe-released-*.png`（松手后应消失）
- 实测（截图确认）：网格 / 十字带 / 中心格都在，读数三行正确
  （选区 700×400pt 时 `区域: 1400 × 800`、`色值: #5D5E61` 与画面一致），松手后放大镜消失
- 修「放大镜开头从左下角滑过来」：图层默认有 0.25s 隐式动画，首次显示就从图层原点 `(0,0)`
  动画到光标处（视觉上就是从屏幕左下角飞过来）。给放大镜所有图层挂上 `loupeNoActions`
  （`position` / `bounds` / `path` / `contentsRect` / `string` … 全 `NSNull`），
  整个 `updateLoupe()` 再包一层 `CATransaction.setDisableActions(true)` —— 全程跟手不插值
- 自检补「跟手」证据：放大镜刚出现后的 t+40 / 140 / 360ms 各打印一次
  `模型 frame` 与 `presentation()` 的**呈现偏差**；实测三次都是 `(0,0)`、模型
  `(916,756 136x136)`（光标 900,700 + 16pt 间距），即**一出现就在光标旁边**

### 钉图浮动按钮在白底上看不清（本轮）
- 问题：两个按钮（右上关闭 / 右下实况文本）用 `NSVisualEffectView(.hudWindow)` +
  **白色图标**，而 `.hudWindow` 是**跟着系统外观**的：浅色外观下它渲染成浅色玻璃，
  白色图标贴在纯白截图上几乎看不到（用户反馈「白色背景下难以分辨」）
- 修：钉图按钮要浮在**任意**截图上，所以固定走深色玻璃：
  - `effectView.appearance = .darkAqua`（不再随外观变浅）
  - 玻璃下面再加一层**压底墨色** `NSBox(black 0.45)`：玻璃会采样底图，采到白色内容时
    单靠材质还是偏亮，压一层才保证白图标在任何底图上都够对比
    （设计系统里浮动表面同样是「磨砂 + scrim」）
  - 边缘高光 0.22 → 0.28、投影 0.25 → 0.35（白底上把按钮「托」出来）
  - 按下反馈（alpha 0.75）也跟着带上 scrim，视觉一致
- 自检 `--selftest-pin <目录>`：钉一张**纯白**图（最坏底图），鼠标挪进窗口后截图 +
  量按钮处亮度。实测：圆盘 **0.365**、白底 1.000（原来几乎与白底同亮度）；
  截图里两个按钮都是「深色圆盘 + 白图标」，一眼可辨
- 回归：`PinWindowTests.pinButtonKeepsDarkBackdrop` 盯住「按钮底必须固定深色」

### 修「放大镜看的地方和鼠标尖不是一个位置」（本轮）
- 现象（用户描述）：鼠标尖停在空白处，放大镜里却全是内容 —— 采样位置整体偏了
- 根因：**`CALayer.contentsRect` 的 y 原点在 macOS 上是图像底边**（非 flipped 图层实测），
  而放大镜按「图像原点左上」直接写了 `y = originY / H` → 采样窗口被**上下镜像**（绕图像中线翻），
  于是显示的是「关于屏幕中线对称的那块」，十字/中心格还留在原地，看起来就是光标与内容错位
  - 顺带：镜像也让「中心格」显示的其实是窗口最后一行，格内内容整体上下颠倒
- 定位手段（都留在自检里）：
  - `--selftest-loupe` 现在会**自动挑一块细节最多的地方**（`BitmapData` 扫相邻像素差），
    合成鼠标移动，然后逐格比对「放大镜里每格的颜色 vs 冻结图同坐标的像素」，
    并在 ±6 像素里搜「哪个偏移能让两边对上」→ 直接报出偏移量
  - 修前：144 格里 96 格对不上、最佳偏移落在搜索边界（镜像）；修后：**最佳匹配偏移=(0,0)、平均差 0.0**
  - 顺带用系统光标位置（`CGEvent(source:).location`）反算像素，与放大镜报的光标像素对账，差 (0,0)
- 回归用例 `JietuTests/ContentRectOriginTests`：
  - `contentsRect 的 y 原点在图像底边`：把这条 quirk 钉住（哪天系统行为变了会先红）
  - `放大镜采样窗口：左上角像素落在图层左上角`：用 8×8 靶图（窗口四角四种颜色）渲染图层，
    断言窗口左上像素出现在图层左上、右下像素出现在图层右下 —— 直接防住翻转 / 错位
- 修法：把窗口 → `contentsRect` 的换算抽成 `LoupeGeometry.contentsRect(sourceOrigin:cells:imageSize:)`，
  y 用 `(H - originY - cells) / H`（换算成「距底边」），放大镜与单测都走这一处

## 命令

```bash
# 构建（工程用自签名证书 Jietu，直接构建即可）
xcodebuild -project Jietu.xcodeproj -scheme Jietu -configuration Debug \
  -destination 'platform=macOS' build

# 单元测试
xcodebuild -project Jietu.xcodeproj -scheme Jietu -configuration Debug \
  -destination 'platform=macOS' test

# 无证书环境可退回临签：加 CODE_SIGNING_ALLOWED=NO

# 自检（先构建，再直接运行二进制）
Jietu --selftest-capture <输出目录>
Jietu --selftest-overlay <持续秒数> <输出目录>
```

## 待完善

- **标注编辑器增强**：移动/删除单条标注（当前只能撤销）、整体缩放/裁剪、更多字体与线型
- **录屏**：尚未实现（滚动长图已实现：自动 + 手动，见上）
- **保存反馈**：保存无独立提示（已用「最近截图」与「打开截图文件夹」间接补偿），
  可考虑菜单栏图标闪烁或通知

### 吸附预览「按下即收」（本轮修复）
- **问题**：鼠标停在一个窗口上，绿色吸附描边好好的；**一按下准备框选，描边却赖在窗口上不动**，
  一直拖到选区成型（越过 3pt 阈值）才不见——整个拖框过程都挂着那个绿圈。
  根因：`updateAllLayers()` 不重算 `windowHighlightLayer`，只有鼠标移动（`updateHoveredWindow`）
  才会重算，而拖动期间不再有 mouseMoved。
- **改法**：新增 `isPressPreviewSuppressed`（`interaction` 为 pressing / selecting / resizing / moving
  时为真）与 `hoveredWindowPreviewRect`；`mouseDown` 设完 `interaction` 立刻
  `updateWindowHighlight()`。收的是**画面**——`hoveredWindow` 状态原样保留，
  「按下不拖动 = 截一个窗口」照旧（自检里钉住）。
- **压暗层的洞口不跟着收**（仍按 `hoveredWindowLocalRect` 算）：按下瞬间就把窗口重新压暗会闪一下，
  那一片该一直亮到选区接管；松手没形成选区时预览会自动回来（`mouseUp` 末尾已经重算）。
- **新增自检** `--selftest-overlay-press`：真弹遮罩 → 找一个「鼠标放上去必定命中」的窗口 → 注入
  按下 / 拖动 / 松手，四段都量：
  - 悬停 → 描边出现（**前置条件**，拿不到就直接 FAIL，不给「按下后隐藏」白通过）
  - **按下 → 描边消失 10~21ms**（含注入投递；图层 `actions` 全是 `NSNull`，应用内是同一次事件里完成的）
  - 拖动四步全程「仍收起」（改之前是全程挂着）
  - 松手成型 → 描边收起；再按一次拖一点点松手（选区被丢弃）→ 描边 5~10ms 回来
  - 单击窗口（不拖动）→ 10ms 截取该窗口
  - 截图 `overlay-press-dragging.png`：窗口边缘绿偏移 0.000（描边确实不在了），选区绿框在场
- **顺手记一个注入管线的坑**：同一位置的「按下 → 抬起」常要等下一次鼠标移动才被投递
  （自检里两次踩到）。要验「按下路径」本身，用合成事件直送遮罩（`debugClick`）比注入稳。

### 就地编辑工具栏加「滚动截图」入口（本轮新增）
- **工具栏**（`InlineMainToolbar`）在「识别文字」和「保存」之间加了 `scroll` 图标的**滚动截图**按钮；
  点一下在它正下方浮出**子面板**（复用颜色 / 粗细那套 `OptionsAnchorReporter` 定位机制）：
  `[scroll] 谁来滚  [hand.draw] 手动滚动  [wand.and.rays] 自动滚动`
  —— 措辞与滚动长图的模式条一致（只回答「谁来滚」），tooltip 说明「自动需要辅助功能权限」
- **一点即开跑**：选了模式就用**当前选区**起会话，不再弹「手动 / 自动」模式条
  （`ScrollingCapturePanelController.present(near:stage:)` 新增初始阶段参数，直接从 `.running` 起，
  避免闪一下模式条）；三个子面板（颜色 / 粗细 / 滚动截图）互斥，与原有行为一致
- **数据流**：`InlineToolbarModel.onScrollCapture(mode)` → `OverlayCanvasView`（收起选项条 + 报选区）
  → `OverlayWindowController` → `OverlayCoordinator.onScrollCapture(快照, 模式, local 矩形)`
  → `AppDelegate.startScrollingCaptureFromInline`（建控制条 / 右侧预览 → 开跑）
- **进取景框时把就地那套 UI 收掉**：`setScrollCaptureChrome(true)` 现在也 `hideToolbar()` +
  隐藏实况文本层与进行中的文字框——取景框期间遮罩是鼠标穿透的，留着它们既点不到也挡视线
- **两条自检**（分工明确）：
  - `--selftest-inline-scroll`：真弹遮罩 → 拖选区 → 点开滚动截图 → **截图**
    （`inline-scroll-options.png`）→ 选手动滚动 → 断言「模式 + 选区」交出去且与选区逐像素一致
  - `--selftest-app-inline-scroll`：**跑在真 App 接线里**（`--selftest` 那批在接线之前就 return 了，
    没有 AppDelegate）。实测：触发→会话起来 **47ms**，控制条可见、遮罩鼠标穿透（取景框）、
    右侧预览挂上；取消后会话与遮罩都收干净
- **新增单测** `InlineToolbarTests`（3 个用例）：模型交模式、滚动选项条渲染与三面板互斥、
  主工具栏量得出尺寸

### 滚动长图：鼠标不再被拽走、预览与自动滚动变顺（本轮修复）
- **「滚动时鼠标挪不动 / 点不到完成、取消」是两处错叠出来的**：
  1. 合成滚轮固定发**选区中心**，而 window server 会把事件里的 `location` 当成新的光标位置
     同步过去 —— 每滚一步就把用户的鼠标吸回中心（探针实测一次滚动拽走 **494~583pt**）；
  2. `makeScrollingTarget` 换算全局 cg 矩形时，把 local 矩形的 origin（**左下角**）直接当成
     cg 的左上角用，**整块矩形往下错了一个高度**：滚轮发去了选区下方，而自动滚动那张
     「输入屏蔽区」也跟着错位、正好盖住控制条 —— 用户点「完成 / 取消」被吞掉的另一半原因。
- **改法**：
  - 滚轮事件位置 = **光标当前所在点**（不再固定中心）：位置与光标一致 → 不再动它，
    滚轮照旧送给光标下面的窗口；探针里「旧做法 vs 新做法」对同一批帧各发 3 步，
    页面位移相当（变化 5.29 / 5.33），光标漂移 **72→0pt**（选区中心口径则是 494~583pt）
  - 新增 `DisplayGeometry.cgRect(fromLocal:screen:)` 做**整块矩形**换算（原先只有点换算），
    并补单测钉住「往返一致」与「老写法正好错一个高度」
  - **光标移出选区即暂停**：不滚、同时把用户输入**全部放行**（`AutoScroller.setActive(false)`），
    移回来继续；控制条写明「已暂停：鼠标在选区外…」。自动模式启动时若光标不在选区里，
    会先把它挪进选区中心一次（「自动」就该点完自己跑）
- **自动滚动更顺**：步长 15% → **8%**（夹 40...120pt）、采样间隔 0.25s → **0.12s**、
  帧数上限 120 → **240**
- **拼接省掉每帧一次 Vision 配准**：自动模式的位移是我们自己发出去的，把已知步长告诉拼接器
  （`expectedShiftPixels`），先用它 + 行签名校验，对得上就不跑 `VNTranslationalImageRegistration`
  —— 自检实测 **17.4ms → 0.98ms/帧**（Vision 侧均值 17.4 / 最大 45.5ms，那正是自动滚动
  一顿一顿的来源）；校验不过（滑到头 / 惯性 / 吸附）照旧回退 Vision，正确性不变
- **右侧预览改成增量小画布**：以前把**整张长图**交给 SwiftUI 缩放（每帧拷贝量随图长线性涨，
  而且 `.fit` 会把长图越缩越细、最后只剩一条竖线）；现在按面板像素尺寸定比例开一块小画布
  （232×800px），每拍只把新增的几行画进去、旧内容整体上移一格 —— 实测 **0.01ms/帧**
  且与长图多长无关（老做法等价 1.7~4.3ms/帧并持续增长），看到的也总是刚滚出来的内容
- **新增 / 更新自检**：
  - `--selftest-scroll-cost`：拼接（Vision / 已知步长）、预览、老做法对比，一次把三个口径量出来
  - `--selftest-scrollprobe`：每个注入变体后面附「光标漂移 pt」——这条就是用户报的那个坑
  - `--selftest-app-inline-scroll` 加自动滚动段（真接线）：光标移出选区→暂停 **406~482ms**、
    移回→恢复 **120ms**、触发即滚动（不是一上来就暂停），实测 PASS

### 右侧预览「刚开始就拉伸」修掉（本轮修复）
- **现象**：长截图刚起步时，右侧预览里的图被横向压扁、铺满整块面板（用户截图）。
- **两个原因**：
  1. `run()` 里**首帧**那次 `onPreview` 发的是**选区原图**（1160×880），绕过了新的小画布；
     预览面板 `thumbnail()` 之后交给 SwiftUI 的 `.resizable()` 直接铺满 116×400 的框
     ——选区是宽扁的、面板是窄高的，于是被拉变形。（自检探针实测：预览图 1160×880 ≠ 画布 232×800）
  2. 画布自身的**排布**也错：首帧画在顶部、后续新增行又钉在底部，中间留一条缝。
- **改法**：
  - 首帧也走画布（`stitcher.currentPreviewImage()`），尺寸恒为 232×800
  - 画布排布改成「从顶部往下码，装不下时整体上移一格」——早期不留缝、之后最新一段总在底部
  - 预览视图保留比例（`.aspectRatio(contentMode: .fit)`）：万一拿到别的比例的图也只是留白，
    不再拉变形
- **验证**：`--selftest-app-inline-scroll` 新增断言「预览走小画布（不是整张长图）」
  （实测 232×800 ✓），并在会话运行中抓一张截图打印路径（`jietu-scroll-preview.png`），
  人眼确认预览比例正常

### 浮窗卡片加「最小尺寸」（本轮）
- **问题**：竖长截图（如 600×1200）或超宽截图按原始比例算出来的卡片只有几十点宽 / 十几点高——
  四角圆盘互相压住、中央「保存」胶囊也放不下（用户截图里就是两个圆盘叠在一起）。
- **改法**：新增 `Theme.Size.quickAccessCardMin` = **96×112**，`QuickAccessView.panelSize(for:)`
  在等比缩放到上限**之后再夹一次下限**；图片改用 `.fit`——卡片正好是图片等比尺寸时铺满，
  被下限夹住时按比例**居中留白**（不裁切、不拉伸）。图标于是永远是同一套「四角 + 中央胶囊」，
  不必为畸形卡片另做一套排版（这点比给图标做多套自适应更省事、也更稳）。
- **为什么是 96×112**：四角两列圆盘要 `2×28 + 两侧 8pt` = 72pt 宽（96 还留 24pt 间隙）；
  中央胶囊（72pt 宽）那一行与上/下两排圆盘不打架需要高 ≥ 100pt（112 留 6pt 余量）。
- **「永不放大」补上（同轮）**：加了最小尺寸之后，几百像素的小图会被撑到卡片尺寸（用户报的
  「截了个很小的图，你给我拉伸得很大」）。现在两处都夹了 `min(1, ...)`：
  - `panelSize(for:)` 收的是截图的**自然点尺寸**（像素 ÷ 屏幕缩放），缩放到上下限之间但不放大；
  - 卡片里的图片按 `imageDisplaySize` 画（等比放进卡片、且不放大）——小图保持原始点尺寸居中留白。
  顺带把点按 / 拖拽热区撑回**整张卡片**（小图边上也能点、能拖），悬停模糊半径也跟着图片尺寸收
  （固定 16 会把几十点的小图直接糊没）。
  ⚠️ 热区手势只能挂在**图片那一层**：挂到外层 ZStack 上，SwiftUI 会把点击装到容器上，
  连 AppKit 那层图标按钮的点击也一起抢走（自检当场量到五个图标全点不动，已改回）。
- **验证**：
  - 新增单测：极端比例夹到最小尺寸（600×1200→96×180、1600×600→260×112、4000×100→260×112）、
    小图不放大（20×10 点的小图 → 卡片 96×112、显示尺寸仍是 20×10）、大图正好填满卡片、
    最小尺寸卡片上五个图标两两不重叠且不越界 —— 共 **128 个用例全绿**
  - `--selftest-quickaccess` 加测三张极端卡片（700×2800→96×180、4000×100→260×112、
    40×24→96×112 的「小图」）：**图标重叠=无、越界=无**，并各留一张截图（`quickaccess-extreme-*.png`）；
    同一轮自检里五个图标的点按延迟照旧全绿（这条就是上面那个「手势抢点击」的守门人）
  - 截图确认：96×180 的窄卡上四角 + 中央胶囊都在、不重叠、不越界

### 钉图加「编辑」按钮：点一下回到标注编辑器（本轮）
- 钉图**左上角**新增 **编辑** 按钮（`square.and.pencil`，与关闭 / 实况文本同款 `GlassControlButton`，
  悬停显隐、tooltip「编辑 (⌘E)」），并支持 **⌘E**（与 ⌘W / Esc 一样在钉图上直接按）。
- 交互：点「编辑」→ **先把钉图收掉**，再把这颗图与钉图位置通过 `PinWindowController.onRequestEdit`
  交给外面（`AppDelegate` 接到 `openAnnotationEditor`）开标注编辑器。
  钉图不依赖 AppDelegate，维持「谁开窗谁负责」的分工；与「编辑器点钉图 → 钉图浮窗」正好对称。
- 命中优先级：编辑 → 关闭 → 实况文本 → 边缘缩放手柄（`hitTest` / `mouseDown` / 光标 / 悬停
  四处各加一档）；退出实况文本时三个按钮一起收起。
- **验证**：
  - 新增单测：左上角「编辑」按钮命中、首击即触发 `onRequestEdit`（共 **129 个用例全绿**）
  - `--selftest-pin` 扩展：白底对比度探针加了「左上编辑」（圆心 0.811 / 圆盘 1.000）；
    末尾再钉一张，验 **点编辑 → 94~100ms 交出 520×340 的图 + 钉图自己收掉**
  - `--selftest-app-inline-scroll`（真接线）加一段：钉图「编辑」→ 标注编辑器**真的打开了**
  - 截图确认三个按钮同款玻璃圆盘、位置不打架（左上编辑 / 右上关闭 / 右下实况文本）

### 矩形 / 椭圆只认边框：框里还能再画框（本轮修复）
- **现象**（用户报的）：画完一个矩形（或椭圆）之后，想在里面再画一个 —— 中间按不下去，
  一按就变成「选中并移动外面那个框」；感觉整个框是**实心一块**而不是一个框。
- **根因**：`Annotation.contains` 把矩形 / 椭圆按**整块矩形**命中
  （`rect.insetBy(-tolerance).contains(local)`），而渲染器只画了边框（`context.stroke`，
  中间是透明的）—— 判定和画面不一致，于是框内全成了「这个框自己的身体」。
- **改法**：矩形 / 椭圆改成**只认那一圈线**：`distanceToRectBorder` / `distanceToEllipseBorder`
  （点到边框的距离，边框上为 0），阈值 `max(tolerance, lineWidth / 2)`。
  高亮 / 模糊 / 马赛克是**实心**的（高亮铺一层色、模糊作用于整块），保持整块命中不变。
  就地标注与标注编辑器共用同一个 `contains`，两边一起修好。
- **影响**：框里可以继续画任何东西；要选中 / 移动已有的框，抓它的**边框**
  （与光标反馈一致）；缩放仍走四角 / 四边的控制点。
- **验证**：
  - 新增单测（共 **131 个用例全绿**）：矩形四条边命中、**框中间不命中**、外面不命中；
    椭圆顶点命中、圆心与「矩形四角」不命中；高亮 / 模糊 / 马赛克仍然整块命中
  - 新增自检 `--selftest-inline-draw`：真在遮罩里就地画两个框（第二个画在第一个**里面**），
    再采样内框上边像素（避开中点那个绿色手柄）——实测四点全是标注红 → **框里画上了**，PASS
  - 截图：`/tmp/jietu-draw/inline-two-rects.png`（内外两个红框都在）

### 屏幕录屏（本轮新增，参考 capcap）
- **入口**：菜单栏「**区域录制 / 窗口录制 / 全屏录制**」（三个各配一个可绑热键动作，与截图那三档对称；
  与截图那五条平铺在同一列，中间用**一条分隔线**隔开）；
  - 区域录制：拖一块区域（顺手点个窗口也行）；窗口录制：相机光标 + 悬停高亮，**点哪个窗口就录哪个**
    （遮罩的 `Purpose.recordWindow`，与截图窗口模式共用选窗逻辑，只是结果取「窗口矩形」而不是截图）；
  - 全屏录制：**不弹遮罩**——鼠标在哪块屏就录哪块，直接上红框 + 控制条；
  三种取景都在选区到手后**停在「准备录制」**——
  红框 + 控制条已经就位，点控制条上的「开始」（或 ⌘⇧S）才真开录。
  不自动开录：录屏是「先把框摆好再决定」，自动开录会把用户还没准备好的那几秒也录进去
  （滚动长图那边也是同一套路）。
- **引擎** `Jietu/Core/Recording/RecordingEngine.swift`：`SCStream` 逐帧 + `AVAssetWriter` 写 mp4
  （与参考项目 capcap 同一条路，没用 macOS 15 的 `SCRecordingOutput`——逐帧才好丢帧 / 暂停 / 自己记时间轴）：
  - 区域录制 = 显示器 filter + `config.sourceRect`；像素 = 区域 × 屏幕缩放，宽高**向上取偶**；
  - HUD / 红框这些自家 chrome 用 `excludingWindows` 排除，**不排除整个 App**（钉图那些照常入镜）；
  - 帧率默认 30（可选 60），H.264 + BT.709，码率按「像素×帧率×0.21」估并夹在 3…80 Mbps
    （`VideoEncodingSettings`，纯函数、单测钉数值）；
  - 系统音频可选（`SCStreamConfiguration.capturesAudio` + AAC 输入），**麦克风暂不支持**；
  - 暂停 = 丢帧 + 把后续 PTS 按**挂钟**前移（用「最后一帧的 PTS」当锚点会把「最后一帧→按下暂停」
    那段真实时间也吞掉：实测暂停 1.2s 却抽掉 1.7s）；恢复后继续录，成片里看不出暂停过；
  - 采集流自己断了（切显示器 / 系统掐流）**能救就救**：已写进去的帧先成片，一帧都没有才算失败。
- **交互**：`RecordingBorderPanel`（区域红框，非交互、被排除）+ `RecordingControlPanel`
  （★ 红点 + `mm:ss` + 暂停/继续 + 完成 + 取消，可拖走）。控制条是 **AppKit** 而不是 SwiftUI：
  非激活面板上 SwiftUI 的 Button 首击可能被用来激活窗口（要按两下），这里用设计系统的
  `GlassControlButton`（`acceptsFirstMouse`）——一下就是一下。
- **快捷键**（录屏期间）：`Esc` 取消、`⌘⇧P` 暂停/继续、`⌘⇧S` 完成；本地 + 全局监视器都装
  （录屏时用户多半在别的 App 里，本地监视器收不到按键；全局监视器只能旁观、拦不住事件）。
- **落盘**：先写临时 mp4，收工才搬到保存目录——**与截图共用**命名模板 / 同名序号
  （`CaptureOutput.moveFile` 复用抽出来的 `uniqueURL`），并弹一条带时长的通知。
- **设置**：新增「录屏」分区（系统音频开关、帧率 30/60、保存位置）。
- **踩到的坑（都写进注释了）**：
  1. 本模块 `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`：跑在采集队列上的类型必须显式
     `nonisolated`，回调闭包必须标 `@Sendable`——否则闭包继承 MainActor 隔离、被丢到后台线程执行，
     运行时隔离断言直接 **SIGTRAP 且没有任何输出**（`queue.async { writer.pause() }` 就是这么炸的）；
  2. 时间轴锚点要用挂钟而不是帧 PTS（见上）。
- **验证**：
  - 单测 **140 个用例全绿**（新增「录屏」4 个：偶尺寸向上取 / 码率模型 / 输出参数 / 落盘命名与序号）
  - `--selftest-record`：同一段画面录两次（不暂停 / 中间暂停 1.5s）比时长 —— 1.47s vs 2.85s
    （暂停被抽掉且后续继续录，**不是** 4.3s），像素 1120×720 = 区域 × 2x，一条视频轨 ✓
  - `--selftest-app-record`（**真接线**）：菜单 → 遮罩框区域 → 会话在录 + 控制条 + 红框 + 遮罩已收
    → 点完成 → 落盘 `Jietu 2026-09-18 at 00.58.59.mp4`（1.85s）→ 控制条收掉 ✓
- **暂未做**：麦克风（需要 AVAudioEngine + CoreAudio 选设备，参考项目那一坨）、mp4→GIF 导出、
  成片裁剪（录完只能整段用）。

### 菜单栏：配了快捷键就显示，没配就不显示（本轮）

- 八个绑热键的动作（区域/窗口/全屏截图、定时截图、滚动长图、区域/窗口/全屏录制）现在会把**当前配的
  快捷键显示在菜单项右侧**；**没配就什么都不显示**。菜单每次弹出重刷一遍（`menuNeedsUpdate` →
  `refresh()`），在设置页改完快捷键，下次打开菜单就是新的。
- 快捷键字符不另做映射表：`Hotkey` 在录制时顺手把 `charactersIgnoringModifiers` 存下来
  （`menuKeyEquivalent`，可选字段，老数据解码成 nil 就不显示），功能键 / 方向键在这套里本来就是
  一个个 Unicode 字符，正好是 `keyEquivalent` 要的。修饰键由 `cocoaModifiers` 从 Carbon 转过来。
- **不会重复触发**：`keyEquivalent` 只当显示用——这份菜单挂在状态栏按钮上、不在 App 的主菜单里，
  AppKit 那套主菜单快捷键匹配够不着它，Carbon 全局热键照旧是唯一的触发者。
- 单测：`Hotkey.from(event:)` 存下字符 / 老数据解码不炸 / 菜单项「配了就显示、清掉就消失」。

### 菜单栏：截图与录制之间加一条分隔线（本轮）

- 顶层顺序：`区域截图 / 窗口截图 / 全屏截图 / 定时截图 ▸ / 滚动长图…` ——分隔线—— `区域录制 / 窗口录制 / 全屏录制`
  然后才是最近截图、打开文件夹、权限、偏好设置、重启、退出。
- **不收起子菜单**：八条动作仍然平铺在顶层，一步就能点到；只用一条分隔线把两家族分清楚
  （试过收进「截图 ▸」「录制 ▸」，多一次悬停不划算，已改回）。
- 单测钉住结构：前九项的标题与分隔线位置逐一断言（`MenuBarTests`），
  免得以后有人把录制那三条挪走、或者把分隔线弄丢。

### 录屏收工的浮窗视频卡（本轮新增）

录完只剩一条通知、文件看不见也拿不走——现在收工后浮窗里会出现一张**视频卡**，与截图同一套表面 / 交互。

- **卡片**（`Jietu/Overlay/QuickAccessVideoView.swift`）：封面是成片里取的一帧，正中挂一枚黑底白字的
  **「▶ 0:12」**胶囊（一眼看出这是段视频、多长）；悬停照样模糊露出毛玻璃、四角浮出同一套
  `GlassControlButton`；**拖出去就是磁盘上那个 mp4**（不是临时文件）。
- **播放 = 用 macOS 自带的「快速查看」**（`QLPreviewPanel`，就是按空格弹出来的那个播放器：
  底下一整条播放控制、右上角「通过 … 打开」）。**点卡片空白处、点中央「播放」都是它**（卡片本体也是）。
  - 不用 .mp4 的默认打开方式：这台机器上默认程序是哔哩哔哩，用户要的是系统那份预览；
    也不用「预览 App」（`Preview.app`）——那是另一个 App，观感与用户要的不是一回事。
  - 我们是后台菜单栏 App：弹面板前先 `NSApp.activate`，否则面板只排在本 App 窗口最前、被别的 App 压住
    （自检截图里拍到过「只有标题栏浮在上面」）。
  - `Info.plist` 补了 `CFBundleLocalizations = [zh-Hans, en]`：系统面板（快速查看、保存面板）的按钮
    之前一律显示英文，因为工程只声明了 `en`；补上后是「通过"哔哩哔哩"打开」这样。
- **动作**：四角复制文件 / 关闭 / **保存** / 在访达中显示，**中央「播放」**。
  - 中央给播放不给保存：静止时那里挂着「▶ 0:12」，悬停后同一位置换成「播放」——同一个意思、
    同一个位置。之前中央是保存，于是**点卡片正中会弹保存面板**（最顺手的那一点反而是错的，用户
    报的就是这个）。
  - 保存（左下那颗）：成片已经在保存目录里了，所以这里是**另存一份**到用户挑的地方（NSSavePanel，
    默认目录与截图「保存」同一个，tooltip 写「另存为…」）；原片留着，卡片上的播放 / 在访达中显示 /
    拖拽都还指着它。
- **封面**（`Jietu/Core/Recording/VideoThumbnail.swift`）：`AVAssetImageGenerator` 取 **0.1 秒**那帧
  （第一帧常是黑场），`maximumSize` 收到 520px 以内（卡片最大才 260×180 点，别为封面解码一整张 4K）；
  时长读文件本身（暂停过的成片按真实时长显示，不用挂钟）。取不到封面就只留通知——收工流程不该被一张封面卡住。
- **落点**：与截图同一个规矩——落在**录的那块屏**的浮窗角落，按同一套尺寸规则排（封面等比、小图不放大），
  计时也走同一个「预览自动关闭」设置。
- **动作层**（`QuickAccessControlsView`）：`QuickAccessAction` 加了 `play / reveal / copyFile`，
  位子抽成 `slot`，卡片用 `imageCard` / `videoCard` 两套动作列表——图片卡那五个一字未动。
- **验证**：单测 **144 个用例全绿**（新增：视频卡五个控件的位子与不重叠（含「中央必须是播放」）/
  时长文本 m:ss 与 h:mm:ss / 封面沿用同一套尺寸规则 / 「保存」把成片交出去且关卡片不删文件 /
  菜单三个录制入口各自走各自回调 / 配了快捷键才显示快捷键 / 老热键数据解码）；`--selftest-app-record` 端到端实测——
  菜单 → 框区域 → **停在待开始**（会话还没起来）→ 点「开始」→ 录 1.5s → 点完成 → 落盘 +
  **浮窗出现视频卡（229×180）**；两张截图 `jietu-recording-ready.png`（待开始的控制条）与
  `jietu-recording-card.png`（封面 + 「▶ 0:02」）都存到临时目录；三种取景也各有一条：
  **全屏录制**不弹遮罩、选框 2560×1440（整块屏）；**窗口录制**点中窗口后选框 = 窗口实际尺寸
  （1618×1162，与窗口一致）。另外钉住三件与「点哪儿都该是预览」有关的事（自检里真实注入点击）：
  **点卡片正中触发的是播放而不是保存**、点空白处 / 点播放按钮都触发预览、
  **浮窗不是 key window 时（`NSApp.deactivate()` 之后）单次点击同样生效**——
  最后这条正是这个 App 反复踩过的坑（首击被用来激活窗口）。
- **注意**：视频卡的 `dragURL` 是用户的成片，关卡片时**不能删**（图片卡那个临时导出文件才删）——
  `Entry.removesDragFile` 就是为这条加的。
- **顺带改的两处**（同一轮反馈）：
  1. 录屏**不再选完即录**：控制条先停在 `phase = .ready`（`[●] 准备录制 [开始] [取消]`），
     点「开始」才起会话；待开始态 Esc / 取消直接收掉红框 + 控制条（这时还没有引擎、没有文件要丢），
     ⌘⇧S 在这一档表示「开始」；
  2. 录屏卡的**操作栏补上「保存」**（见上）。

### 钉图：左下角「翻译」（走 macOS 自己的翻译）+ 右下角「识别文本」蓝底样式（本轮新增）

- **左下角「翻译」**（`Jietu/UI/Translation/SystemTranslationPresenter.swift`）：点一下先
  `OCRService` 认字，再把原文交给 **macOS 自己的翻译面板**——`Translation.framework` 的
  `translationPresentation`（系统那个能选语言、能朗读、能替换的面板）。macOS 截图预览窗口里那颗
  「翻译」也是这个路子：先 Live Text 认字，再翻译。
  - 面板只有 SwiftUI 入口，所以钉图里挂一层透明宿主（贴在按钮上，面板从按钮下方长出来）；
  - 识别期间按钮压暗 + 换提示语：这台机器上量到文字识别**首次**要先装模型（12–14 秒；装完同进程
    内 ≤0.6 秒），没反馈用户只会以为点坏了、接着猛点；
  - 图里没字 → 提示「没识别到文字」；识别回来时钉图已经收掉 → 不弹面板；
  - 按钮本身是胶囊（图标 + 文字），与浮窗中央那颗「保存」同一套 `GlassControlButton`。
- **右下角「识别文本」**：关着是玻璃圆盘，点亮换成 **macOS 那颗 Live Text 的蓝底实心圆**
  （`GlassControlButton.Prominence.accent`：品牌蓝 + 白图标，不再换绿勾——「亮着」由外形直接说）。
  右键菜单同步改叫「识别文本 / 退出识别文本」，并加了「翻译…」。
- **`--selftest-pin` 补上这两颗按钮**：四角按钮在白底上的对比度、识别文本点亮的蓝偏移（B−R
  0.000 → 0.541，圆盘边缘 0.838）、点「翻译」后系统翻译面板真的弹出来（截图 `pin-translate.png`）。
  自检里加了两件东西：`blueBias` 探针，以及一张**有字**的测试图（识别要先有字可认）。
- **验证**：单测 **137 个用例全绿**（新增：翻译按钮接线 —— 悬停出现 / 点中落点 / 原文交给系统翻译；
  蓝底识别文本点亮与还原）。
- **注意**：文字识别走的是既有 `OCRService`（Vision），首次调用要装模型，所以第一次点「翻译」会慢。
- **试过又回退的**：钉图 / 浮窗一度改成「截图内缩 8pt + 一圈玻璃外框 + 18pt 大圆角」的预览卡片外观
  （`PreviewCard`，`PinGeometry` 加了 `contentInset`，`QuickAccessView` 按内容区算尺寸），
  按用户意见**整体回退**，截图依旧铺满窗口（那批文件现在与改动前逐字节一致）。
  要再捡起来的话，`PinImageView` 那条教训值得记：AppKit 的 `draw` 落在子视图**下面**，
  卡片玻璃会把父视图画的截图整张糊成白的，截图必须画在自己的子视图里。

## 需要手工验证（无法单测）

- 窗口截图 / 全屏截图在混合 DPI、多屏下的像素与缩放
- 定时截图与输入法/全屏 Space 的交互
- 「登录时启动」在自签名证书下能否被系统正常登记
- 最近截图子菜单弹出刷新与「清除记录」
- 四个默认热键在不同机器上是否都能注册成功（`⌘⇧W` / `⌘⇧F` / `⌘⇧T` 可能被其它 App 占用）
- 系统通知在自签名 Debug 包下能否正常弹出与携带缩略图
- 滚动长图：不同缩放 / 多屏下的拼接质量
  （坐标系已用 `--selftest-region` 证实：显示器内、原点左上、单位点，主/副屏各 2x 都对得上）
- 钉图「翻译」在**没下载过语言包**的机器上首跑：系统面板的「下载」提示、耗时与最终译文

## 已知设计取舍

- 权限在进程启动时固定（`AppDelegate.hasUsableScreenCapturePermission`）：
  授权后必须重启进程，运行中撤销只有失败弹窗兜底
- 多屏不支持跨屏选区，每块屏独立遮罩
- 签名：工程用自签名证书 **Jietu**（登录钥匙串，`Authority=Jietu`、`TeamIdentifier` 为空、
  hardened runtime 关闭）。发布/分发前需换 Developer ID 并公证

## 已知可优化项

- `PixelSampler.sample` 每次走一遍 CoreGraphics（调用方已节流）
- `AppDelegate` 用 `UserDefaults` 存设置，未做迁移版本
